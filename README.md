# Jwt_With_Role
JWT is a compact, URL-safe token representing claims between two parties, commonly used for authentication and authorization. Upon user login, a JWT is generated containing user details (e.g., username, roles, expiration). Roles like ADMIN, USER, or MODERATOR control access based on assigned permissions.
